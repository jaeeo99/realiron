angular
  .module('RealIron');

// snippet code
//  .filter('scheduleTitle', scheduleTitle)

//function scheduleTitle(){
//  return function(schedule){
//    if(isNull(schedule)){
//      return;
//    }
//    var totalTime = 0;
//    var totalIndex = 0;
//    for(var i = 0 ; i < schedule.length ; i++){
//      if(!schedule[i].hidden){
//        totalTime += convertTimeToInt(schedule[i].play_length);
//        totalIndex += 1;
//      }
//    }
//    return totalIndex + "개의 컨텐츠 / 전체 재생 시간 - " + convertToSecondString(totalTime);
//  }
//}
